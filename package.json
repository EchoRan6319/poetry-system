{
  "name": "poetry-system-root",
  "private": true,
  "workspaces": [
    "src"
  ],
  "scripts": {
    "dev": "npm run dev --workspace src",
    "build": "npm run build --workspace src",
    "preview": "npm run preview --workspace src"
  }
}
